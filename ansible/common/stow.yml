- name: Stow itself
  command: stow -vv -t ~ -d {{playbook_dir}}/.. stow

- name: Stow ZSH
  command: stow -vv -t ~ -d {{playbook_dir}}/.. zsh

- name: Stow NeoVim
  command: stow -vv -t ~ -d {{playbook_dir}}/.. nvim

- name: Stow Tmux
  command: stow -vv -t ~ -d {{playbook_dir}}/.. tmux

- name: Stow Ghostty
  command: stow -vv -t ~ -d {{playbook_dir}}/.. ghostty

- name: Stow Workmux
  command: stow -vv -t ~ -d {{playbook_dir}}/.. workmux

- name: Stow Kimchi
  command: stow -vv -t ~ -d {{playbook_dir}}/.. kimchi
